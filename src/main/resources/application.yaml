spring:
  application:
    name: smartfacility

  datasource:
    url: jdbc:postgresql://localhost:5432/smart_facility
    username: app
    password: app

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  server:
    port: 8080